#include <stddef.h>
#include <stdint.h>

typedef uint8_t u8; // explicitly unsigned 8 bit integer
// typedef char16_t c16; // 16-bit character, UTF-16
typedef int32_t b32; // bool
typedef int32_t i32;
typedef int64_t i64;
typedef uint32_t u32;
typedef uint64_t u64;
typedef float f32;
typedef double f64;
typedef uintptr_t uptr;
typedef char byte; // signed/unsigned per system
typedef ptrdiff_t size;
typedef size_t usize; // niche
